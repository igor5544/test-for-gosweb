<header class="header">
    <div class="header__top">
        <h1>Header</h1>
    </div>

    <div class="header__bottom">
        <nav class="nav">
            <ul class="nav__list">
                {{#navItems}}
                    <li class="nav__item">
                        <a href="{{url}}" class="nav__link">{{title}}</a>
                    </li>
                {{/navItems}}
            </ul>
        </nav>
    </div>
</header>